#include <iostream>
#include <string>

using std::cin;
using std::cout;
using std::endl;
using std::string;

bool compare(matrix &m1, matrix &m2);
// compare wether m1 is equal to m2

vector<int>::iterator change_val(int num, vector<int>::iterator iter);
// change the element iter point to become num and return the iterator

int main()
{
    return 0;
}