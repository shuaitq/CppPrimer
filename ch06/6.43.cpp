#include <iostream>
#include <string>

using std::cin;
using std::cout;
using std::endl;
using std::string;

inline bool eq(const BigInt&, const BigInt&);
// this is a inline function, should be put in header

void putValues(int *arr, int size);
// this is a statement, should also put in header

int main()
{
    
    return 0;
}