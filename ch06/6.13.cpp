#include <iostream>

using std::cin;
using std::cout;
using std::endl;

void f(T);
// the parameter will copy by value

void f(&T);
// the parameter will be the argument's reference

int main()
{
    
    return 0;
}