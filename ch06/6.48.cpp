#include <iostream>
#include <cassert>
#include <string>

using std::cin;
using std::cout;
using std::endl;
using std::string;

int main()
{
    string s, sought;

    while(cin >> s && s != sought)
    // loop until input word sought
    {

    }

    assert(cin);
    // assert(s == sought); is better

    return 0;
}