#include <iostream>

using std::cin;
using std::cout;
using std::endl;

int main()
{
    if(c1 < c2)
    {
        // c1 must have the same type of c2
        // the value_type must have < operator
    }
    return 0;
}