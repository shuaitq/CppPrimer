#include <iostream>
#include <list>

using std::cin;
using std::cout;
using std::endl;
using std::list;

int main()
{
    list<string> lst;

    list<string>::const_iterator read;
    // use const_iterator for read

    list<string>::iterator write;
    // use iterator for write

    return 0;
}