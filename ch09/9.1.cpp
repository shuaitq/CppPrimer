#include <iostream>

using std::cin;
using std::cout;
using std::endl;

int main()
{
    // a list is the best, because you need to insert in the middle

    // b deque is the best, because you have to insert in the end and delete at front

    // c vector is the best, because you can insert them all then sort

    return 0;
}