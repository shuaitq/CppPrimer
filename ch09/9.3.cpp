#include <iostream>

using std::cin;
using std::cout;
using std::endl;

int main()
{
    // they refer to elements of the same container

    // it is possible to reach end by reppeatedly incrementing begin

    return 0;
}