#include <iostream>

using std::cin;
using std::cout;
using std::endl;

int main()
{
    int i, j, k;

    if(i != j < k)
    // is equal to i != (j < k)
    {
        // first calc j < k get the result true(1) or false(0)
        // than calc i != result
    }

    return 0;
}