#include <iostream>

using std::cin;
using std::cout;
using std::endl;

int main()
{
    float fval;
    double dval;
    int ival;

    if(fval)
    // float convert to bool
    {

    }

    dval = fval + ival;
    // ival convert to float, then the result convert to double

    dvel + ival * cval;
    // cval convert to int, then cval and ival convert to double

    return 0;
}