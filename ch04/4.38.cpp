#include <iostream>

using std::cin;
using std::cout;
using std::endl;

int main()
{
    double slope = static_cast<double>(j / i);
    // j / i is an int, then convert to double and assigned to slope

    return 0;
}