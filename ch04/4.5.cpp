#include <iostream>

using std::cin;
using std::cout;
using std::endl;

int main()
{
    // -30 * 3 + 21 / 5
    // (-30 * 3) + 21 / 5
    // -90 + 4
    // -86

    cout << -30 * 3 + 21 / 5 << endl;

    // -30 + 3 * 21 / 5
    // -30 + ((3 * 21) / 5)
    // -30 + (63 / 5)
    // -30 + 12
    // -18

    cout << -30 + 3 * 21 / 5 << endl;

    // 30 / 3 * 21 % 5
    // ((30 / 3) * 21) % 5
    // (10 * 21) % 5
    // 210 % 5
    // 0

    cout << 30 / 3 * 21 % 5 << endl;

    // -30 / 3 * 21 % 4
    // ((-30 / 3) * 21) % 4
    // (-10 * 21) % 4
    // -210 % 4
    // -(210 % 4)
    // -2

    cout << -30 / 3 * 21 % 4 << endl;

    return 0;
}