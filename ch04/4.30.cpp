#include <iostream>

using std::cin;
using std::cout;
using std::endl;

int main()
{
    // sizeof x + y = sizeof(x) + y

    // sizeof p->mem[i] = sizeof(p->mem[i])

    // sizeof a < b = sizeof(a) < b

    // sizeof f() = sizeof(f())

    return 0;
}