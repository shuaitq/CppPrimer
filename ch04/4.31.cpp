#include <iostream>
#include <vector>

using std::cin;
using std::cout;
using std::endl;
using std::vector;

int main()
{
    auto cnt = ivec.size();

    for(auto ix = 0; i x != ivec.size(); ix ++, cnt ++)
    {
        ivec[ix] = cnt;
    }

    return 0;
}