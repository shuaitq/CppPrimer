#include <iostream>
#include <string>

using std::cin;
using std::cout;
using std::endl;
using std::string;

int main()
{
    if(read(read(cin, data1), data2))
    {
        // read data1 first and then read data2, in the end check the value of cin
    }
    return 0;
}