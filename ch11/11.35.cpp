#include <iostream>

using namespace std;

int main(int argc, char *argv[])
{
    // trans_map[key] = value.substr(1);
    // this will always be the last value

    // trans_map.insert({ key, value.substr(1)});
    // this will be the first value

    return 0;
}