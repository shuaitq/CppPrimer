#include <iostream>
#include <map>
#include <string>

using namespace std;

int main()
{
    // find out how must that elements in this multimap i will use count

    // find out if there have that element in this multimap i will use find

    return 0;
}