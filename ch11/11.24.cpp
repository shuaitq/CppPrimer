#include <iostream>
#include <map>

using namespace std;

int main()
{
    map<int, int> m;
    m[0] = 1;

    // check key_value 0 is in this map
    // if not create it and init mapped_value become 0
    // make mapped_value become 1

    return 0;
}