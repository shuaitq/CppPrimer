#include <iostream>

using namespace std;

int main(int argc, char *argv[])
{
    // const string &transform(const string &s, const map<string, string> &m)
    // {
    //     return m[s];
    // }
    
    // this won't compile because this subscript operator might insert an element
    // and map is only const

    return 0;
}