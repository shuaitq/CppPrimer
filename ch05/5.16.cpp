#include <iostream>

using std::cin;
using std::cout;
using std::endl;

int main()
{
    // for(int ix = 0; ix != sz; ++ ix)
    // {
    // }
    // if(ix != sz)
    // {
    // }

    int ix;
    for(ix = 0; ix != sz; ++ ix)
    {
    }
    if(ix != sz)
    {
    }

    // int ix;
    // for(ix != sz; ++ ix)
    // {

    // }

    int ix;
    for(ix = 0; ix != sz; ++ ix)
    {
    }

    // for(int ix = 0; ix != sz; ++ ix, ++ sz)
    // {
    // }

    for(int ix = 0; ix != sz; ++ ix)
    {

    }

    return 0;
}