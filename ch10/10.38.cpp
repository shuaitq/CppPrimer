#include <iostream>

using namespace std;

int main(int argc, char *argv[])
{

    // input iterators: istream_iterator ==, !=, ++, *, ->

    // ouput iterators: ostream_iterator ++, *

    // forward iterators: forward_list::iterator ==, !=, ++, *, ->

    // bidirectional iterator: list::iterator ==, !=, ++, --, *, ->

    // random-access iterator: vector::iterator ==, !=, <, <=, >, >=,++, --, +, +=,
                                             // -, -=, -, *, ->, iter[n] = *(iter + n)

    return 0;
}