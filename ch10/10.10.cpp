#include <iostream>

using std::cin;
using std::cout;
using std::endl;

int main()
{
    // because algorithms operate on iterators, not containers
    // algorithm connot directly add or remove elements

    return 0;
}