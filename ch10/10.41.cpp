#include <iostream>

using namespace std;

int main(int argc, char *argv[])
{
    // replace(beg, end, old_val, new_val);
    // from beg to end replace old_val to new_val

    // replace_if(beg, end, pred, new_val);
    // from beg to end if pred is true replace to new_val

    // replace_copy(beg, end, dest, old_val, new_val);
    // copy from beg to end and if old_val replace to new_val

    // replace_copy_if(beg, end, dest, pred, new_val);
    // copy from beg to end and if pred is true replace to new_val

    return 0;
}