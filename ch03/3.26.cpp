#include <iostream>
#include <vector>

using std::cin;
using std::cout;
using std::endl;
using std::vector;

int main()
{
    // because when beg and end are very large
    // beg + end will be overflow
    // use beg + (end - beg) / 2 can avoid it

    return 0;
}